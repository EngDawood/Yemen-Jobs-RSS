# Render Service Information

## Service Details

- __Name:__ Yemen-Jobs-RSS
- __ID:__ `srv-d2sbnibipnbc7388kldg`
- __Environment:__ Docker
- __Repository:__ `https://github.com/EngDawood/Yemen-Jobs-RSS`
- __Deployment Branch:__ `main`
- __Public URL:__ `https://yemen-jobs-rss.onrender.com`
- __Dockerfile:__ The service is built using the `Dockerfile` in the root of your repository.

## How to Update the Service

Your service has __auto-deploy enabled__ from the `main` branch. This makes updates very simple:

1. __Make your changes:__ Modify the code in your local repository as needed.
2. __Commit and push:__ Commit your changes and push them to the `main` branch of your GitHub repository.

Render will automatically detect the changes, rebuild your Docker container, and deploy the new version of your application.

**
